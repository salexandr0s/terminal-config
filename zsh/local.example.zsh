# Put machine-specific aliases, functions, or non-secret overrides here.

# Examples:
# alias dev="cd ~/GitHub"
# export EDITOR="nvim"

# Keychain-backed env vars can live here if you want them available in
# interactive shells.
# export AGENTMAIL_API_KEY="$(security find-generic-password -s "agentmail_api_key" -w ~/Library/Keychains/login.keychain-db 2>/dev/null)"
